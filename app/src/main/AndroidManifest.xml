<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asistmed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsistMed"
        android:usesCleartextTraffic="true">
        <activity android:name=".AdministradorActivity" />
        <activity android:name=".RecuperarContrasenaActivity" />
        <activity android:name=".LoginActivity2" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".BienvenidaActivity" />
        <activity android:name=".PruebasAlberto" />
        <activity android:name=".MedicamentosActivity" />
        <activity android:name=".AddTratamientosActivity" />
        <activity android:name=".AsistentActivity" />
        <activity android:name=".TratamientosActivity" />
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>